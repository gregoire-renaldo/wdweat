<%= form_for(@selection) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :date %>
  <%= f.datetime_select :date, :ampm => true, :minute_step => 15 %>

  <%= f.submit %>
<% end %>
